# version: '3.8'
# services:
#   react-app:
#     container_name: react-dashboard
#     image: react-dashboard
#     restart: always
#     build:
#       context: ./sri-frontend
#       dockerfile: Dockerfile_front
#     volumes:
#       - ./sri-frontend/src:/app/src
#     ports:
#       - '3000:3000'
#     #env_file:
#       #- config/my-env

#   backend:
#     build:
#       context: .  # Assuming the backend Dockerfile is in the root
#       dockerfile: Dockerfile  # This is the Dockerfile for FastAPI
#     volumes:
#       - .:/app  # For live reloading of backend code
#     ports:
#       - '8000:8000'
#     environment:
#       - DATABASE_URL=postgresql://postgres:postgres@db:5432/mydatabase
#     depends_on:
#       - db

#   db:
#     image: postgres:13
#     environment:
#       POSTGRES_USER: postgres
#       POSTGRES_PASSWORD: postgres
#       POSTGRES_DB: mydatabase
#     volumes:
#       - postgres-data:/var/lib/postgresql/data
#     ports:
#       - '5432:5432'

# volumes:
#   postgres-data:

version: "3.3"

services:
  app:
    container_name: react-dashboard
    image: react-dashboard
    restart: always
    build:
      context: ""
    volumes:
      - ./src:/app/src
    ports:
      - '3000:3000'
    stdin_open: true
